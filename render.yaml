services:
  - type: web
    name: shadow-code-auditor
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python backend/app.py
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false  # ستُدخلها يدوياً في لوحة التحكم
    plan: free
